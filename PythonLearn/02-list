1. 列表 list 
    （1）定义：用 [ ] 夹住内容，表示列表。like, adcs = [123,'2343',22.1]
    （2）变量名：尽量使用复数
    （3）结尾不需要分号
    （4）列表内容可以是字符串、数字、浮点数
    （5）列表基础操作
        查询：使用 列表名[index] 查询，可以用[-1]查看列表最后一个元素
        增加：append(元素值) --在列表尾部添加；
            insert(索引,元素值)，在指定位置添加，其后元素逐个往右移动。 
        删除：del 列表名称 --默认删除整个列表，del 列表名称(index) --删除指定位置元素；
            pop(),所删除的值可赋值给一个变量，默认删除列表尾部元素；pop(index),删除指定位
            置元素。
            remove(),删除指定_值_元素，必须加参数使用,且值不能赋值给变量使用。
            比如：remove("kobe")
        修改: 使用索引和赋值语句直接修改，如：列表名[index]=new_value
    (6)列表管理：
        i. 列表永久排序： 列表名.sort() --按字母顺序，从前往后排序；
            列表名.sort(reverse=true) ---按字母顺序，从后往前排序
        ii.列表临时排序： 列表实际存储顺序不变 列表名.sorted() --按字母顺序，从前往后排序；
            sorted(listName,reverse=true) ---按字母顺序，从后往前排序,
            like, sorted(bicycles,reverse=True)
        iii.倒着打印列表（永久修改列表顺序）： 列表名.reverse() ---将列表存储顺序反转
        iiii.列表长度：使用函数len(listName) 来计算，比如：len(bicycles)列表里有5个元素，那结果就是5 
    (7)列表操作
        i. for循环遍历列表. 第一行最后的冒号必须有。
            for itemName in listName:
                print(itemName)
    (8)常用函数
        range(1,5) ，值 >=1 ,<5
        list(),参数输入可以是数字、字符串，输出是列表。
        min(),输出列表中的最小值
        max()，输出列表中的最大值
        sum()，输出列表中所有值的和
        list comprehension, eg,squares = [square**2 for square in range(1,11)]
2. 元组 tuple
    （1）定义：用(a，b)来定义元组，标志是逗号，元括弧是为了看起来更整洁。
    （2）特征：
        a. 一般是至少两个元素组成，但是自动生成的元组会只有一个元素的情况，这样的话就是一
        个元素后面跟逗号来表示, like (a,)
        b. 元组的元素值不能被改变，从一而终。如果想改变元素值，只能给元组变量重新赋值。比如：
        tuple1 = (100,2) 修改元素值的话，就 tuple1 = (100,3)
        c. 列表, 非常适合用于,存储在程序运行期间可能变化的数据集. 元组适合存,不变的数据。
    （3）遍历，使用for循环。比如：for item in tuple1 :
3. 编码格式：
    （1）每行代码不超过79个字符
    （2）每行注释不超过72个字符
    （3）不要滥用空白行，功能之间分割时需要
    （4）缩进是4个空白字符，tab键设置为4个空白。