列表 list 
    （1）定义：用 [ ] 夹住内容，表示列表。
    （2）变量名：尽量使用复数
    （3）结尾不需要分号
    （4）列表内容可以是字符串、数字、浮点数
    （5）列表基础操作
        查询：使用 列表名[index] 查询，可以用[-1]查看列表最后一个元素
        增加：append(元素值) --在列表尾部添加；
            insert(索引,元素值)，在指定位置添加，其后元素逐个往右移动。 
        删除：del 列表名称 --默认删除整个列表，del 列表名称(index) --删除指定位置元素；
            pop(),所删除的值可赋值给一个变量，默认删除列表尾部元素；pop(index),删除指定位置元素。
            remove(),删除指定值元素，必须加参数使用,且值不能赋值给变量使用。比如：remove("kobe")
        修改: 使用索引和赋值语句直接修改，如：列表名[index]=new_value
    (6)列表管理：
        i. 列表永久排序： 列表名.sort() --按字母顺序，从前往后排序；列表名.sort(reverse=true) ---按字母顺序，从后往前排序
        ii.列表临时排序： 列表实际存储顺序不变 列表名.sorted() --按字母顺序，从前往后排序；sorted(listName,reverse=true) ---按字母顺序，从后往前排序,like sorted(bicycles,reverse=True)
        iii.倒着打印列表（永久修改列表顺序）： 列表名.reverse() ---将列表存储顺序反转
        iiii.列表长度：使用函数len(listName) 来计算，比如：列表里有5个元素，那结果就是5 
    (7)列表操作
        i. for循环遍历列表. 第一行最后的冒号必须有。
            for itemName in listName:
                print(itemName)